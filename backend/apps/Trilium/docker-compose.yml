services:
  trilium:
    image: triliumnext/trilium:latest
    container_name: trilium
    restart: unless-stopped
    ports:
      - '8082:8080'
    volumes:
      - ~/trilium-data:/home/node/trilium-data
    environment:
      - TZ=America/Santiago
    labels:
      homelab.name: Trilium
